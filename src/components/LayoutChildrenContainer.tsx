import styled from 'styled-components';
import { Box } from "@primer/components";

const LayoutChildrenContainer = styled(Box)`
  flex: 1 0 auto;
  width: 95%;
  margin-right: auto;
  margin-left: auto;
`;

export default LayoutChildrenContainer;
